# MarketPanel 機能仕様書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| アプリ名 | MarketPanel |
| パッケージ名 | `com.digihori.marketpanel` |
| 対象 | Androidタブレット |
| 画面方向 | 横画面固定 |
| 文書基準日 | 2026年8月16日 |
| 文書状態 | 現行実装仕様 |

## 2. 目的

MarketPanelは、使用していないAndroidタブレットを、株価・ETF・市場指標を常時表示するスマートモニターとして再利用するためのアプリである。

ユーザーが登録した金融商品を3つのパネルへ分類して表示し、一定間隔で自動的に切り替える。実データ取得時は、APIキーをAndroid端末へ保存せず、Cloudflare Workerを経由してTwelve Dataへアクセスする。

## 3. システム構成

```text
Androidタブレット
  ├─ ダッシュボード表示
  ├─ 銘柄・動作設定
  ├─ Roomキャッシュ
  ├─ DataStore設定
  ├─ APIクレジットログ
  └─ API失敗・停止状態
          │ HTTPS
          ▼
Cloudflare Worker
  ├─ Twelve Data APIキーの保持
  ├─ APIレスポンス変換
  ├─ Cloudflare Cache
  └─ Workers KV
          │
          ▼
Twelve Data API
```

## 4. ビルドバリアント

| Variant | データモード | Application ID | 用途 |
|---|---|---|---|
| `debug` | デモ | `com.digihori.marketpanel.debug` | UI、グラフ、ローテーション確認 |
| `liveDebug` | 実データ | `com.digihori.marketpanel.live` | 実データ接続の確認 |
| `release` | 実データ | `com.digihori.marketpanel` | 配布用 |

`debug`と`liveDebug`は別アプリとして同一端末へ共存できる。

## 5. 画面一覧

| 画面 | 目的 | 起動方法 |
|---|---|---|
| ダッシュボード | 金融情報の常時表示 | アプリ起動 |
| 設定 | 表示・銘柄・端末動作の変更 | ダッシュボードを長押し |
| APIログ | API呼び出し履歴の確認 | 設定画面の「APIログ」 |

## 6. ダッシュボード仕様

### 6.1 画面構成

画面上部にAPI使用量、中央から下部に3つの情報パネル、最下部にローテーション進捗バーを配置する。

```text
┌──────────────────────────────────────┐
│                         API 123 / 800│
├──────────────────────────────────────┤
│                                      │
│                MAIN                  │
│                                      │
├───────────────────┬──────────────────┤
│       SUB1        │       SUB2       │
├───────────────────┴──────────────────┤
│ ローテーション進捗バー              │
└──────────────────────────────────────┘
```

### 6.2 パネル分類

| パネル | 表示対象 |
|---|---|
| MAIN | 米国株、米国ETF |
| SUB1 | 国内投信の参考指標 |
| SUB2 | 市場指数、為替 |

銘柄設定の「種別」に基づいて表示先を決定する。

### 6.3 表示項目

各パネルに以下を表示する。

- パネル分類ラベル
- シンボル
- 銘柄名または参考対象名
- 取引所・補足情報
- 現在値または最新値
- 騰落額
- 騰落率
- 更新時刻
- 価格推移グラフ
- 縦軸単位
- 横軸年月
- データ取得状態

### 6.4 色

| 対象 | 色 | 意味 |
|---|---|---|
| 騰落文字 | 緑 | 値上がりまたは前日比0以上 |
| 騰落文字 | 赤 | 値下がり |
| グラフ | 青 | 値上がりまたは前日比0以上 |
| グラフ | 赤 | 値下がり |
| 取得状態 | 緑 | その時点まで取得失敗なし |
| 取得状態 | 赤 | 1件以上の取得失敗あり |

騰落色はグラフ期間全体の始点・終点比較ではなく、最新の騰落情報に基づく。

### 6.5 グラフ

- 標準期間は1年・週足とする。
- 横軸は年月を表示する。
- 縦軸は通貨または指数値を表示する。
- データ最小値・最大値に対し、データ範囲の20%を上下へ加えた値を描画範囲とする。
- 値が一定の場合は、値の5%または1の大きい方を上下余白とする。
- 銘柄名が長い場合は最大2行へ折り返し、超過部分を省略記号で表示する。

### 6.6 取得状態表示

取得中は次の形式で表示する。

```text
取得中 3/6 • 成功2 • 失敗1
```

完了後は次の形式で表示する。

```text
オンライン • 表示可能6/6
キャッシュ • 表示可能6/6
一部取得失敗 • 表示可能4/6
オフライン • 保存データ
```

表示可能な銘柄が1件の場合、ローテーションタイマーは動作しても表示内容は変化しない。

## 7. ローテーション仕様

### 7.1 選択可能時間

- 5秒（デバッグ用）
- 30秒
- 60秒
- 3分
- 5分

### 7.2 パネル間の時間差

通常設定では、切り替えタイミングを次のようにずらす。

| パネル | 基準からの時間差 |
|---|---:|
| MAIN | 0秒 |
| SUB1 | 10秒 |
| SUB2 | 20秒 |

5秒（デバッグ用）では時間差を適用せず、3パネルを同時に切り替える。

各パネルは起動直後に最初のデータを表示する。時間差は最初の次銘柄への切り替えから適用する。

### 7.3 データ追加時

実データを段階的に取得した場合、取得済み一覧へ銘柄を追加する。データ到着によって進行中のローテーションタイマーをリセットしない。

### 7.4 プログレスバー

- MAINの切り替え周期を表示する。
- 0から100%まで一定速度で進む。
- MAINが次銘柄へ切り替わると0へ戻る。
- 設定変更時は古いアニメーションと保留中タイマーを停止し、新しい周期で0から再開する。

## 8. 初期銘柄

### 8.1 米国株・ETF

| 表示名 | シンボル | 種別 | 標準供給元 |
|---|---|---|---|
| IBM | IBM | 米国株 | Twelve Data |
| McDonald’s | MCD | 米国株 | Twelve Data |
| SpaceX | SPCX | 米国株 | Twelve Data |
| JEPQ | JEPQ | 米国ETF | Twelve Data |
| QQQI | QQQI | 米国ETF | Twelve Data |
| 563A 国内ETF | 563A | 米国ETF扱い | デモ／手動 |

### 8.2 国内投信参考

| 表示名 | 参考シンボル | 標準供給元 |
|---|---|---|
| オルカン参考 | ACWI | 参考指標（円換算） |
| FANG+参考 | FNGS | 参考指標（円換算） |
| SBI・S・米国高配当参考 | SCHD | 参考指標（円換算） |
| Tracers 日経高配当50参考 | NIKKEI225 | Twelve Data |
| eMAXIS Slim S&P500参考 | VOO | 参考指標（円換算） |

参考指標は投資信託そのものの基準価額ではなく、連動対象に近いETFまたは指数の推移を表示する。

### 8.3 市場指数・為替

| 表示名 | ID | Twelve Dataシンボル |
|---|---|---|
| 日経平均 | NIKKEI225 | N225 |
| S&P 500 | SP500 | SPX |
| 米ドル／円 | USDJPY | USD/JPY |

## 9. 設定画面仕様

### 9.1 動作設定

- 表示切り替え間隔
- データ更新間隔
- 端末起動時の自動起動
- 画面点灯維持
- 全画面表示

### 9.2 銘柄設定

各銘柄について以下を設定できる。

- 有効・無効
- 表示名
- シンボル
- 種別
  - 米国株
  - 米国ETF
  - 指数・為替
  - 国内投信参考
- データ供給元
  - Twelve Data
  - 参考指標（円換算）
  - デモ／手動
- 表示順

銘柄の追加、編集、削除、上移動、下移動を可能とする。

設定はDataStoreへ保存し、アプリ再起動後も保持する。初期銘柄の追加・供給元変更がある場合は、設定バージョンに基づいて既存設定を移行する。

## 10. データ取得仕様

### 10.1 AndroidからWorkerへのAPI

| パス | 用途 | 推定クレジット |
|---|---|---:|
| `/v1/quotes/{symbol}` | 現在値 | 1 |
| `/v1/charts/{symbol}` | 週足チャート | 1 |
| `/v1/markets/{id}` | 指数の現在値と系列 | 2 |
| `/v1/usage` | API使用量 | 1 |

### 10.2 Workerの役割

- AndroidへTwelve Data APIキーを配布しない。
- Android向けの安定したJSON形式へ変換する。
- 市場IDをTwelve Dataのシンボルへ変換する。
- 成功レスポンスをCloudflare CacheとWorkers KVへ保存する。
- エラーレスポンスはキャッシュしない。
- Twelve DataのHTTPエラーコードを可能な限り維持する。

### 10.3 API使用量

ダッシュボード上部に次の形式で表示する。

```text
API 123 / 800
```

デモモードでは次を表示する。

```text
API DEMO
```

## 11. キャッシュ仕様

### 11.1 Android Room

| データ | 有効期間 |
|---|---:|
| 現在値 | 2時間 |
| 週足チャート | 24時間 |
| 市場指標 | 2時間 |

- 有効期間内はWorkerへアクセスせず、Roomのデータを利用する。
- 有効期限後の取得に失敗した場合は、期限切れデータを保存データとして表示できる。
- アプリ更新と端末再起動ではキャッシュを保持する。
- アプリのデータ消去またはアンインストールで削除される。

### 11.2 Cloudflare

| データ | KV有効期間 |
|---|---:|
| 現在値 | 2時間 |
| 市場指標 | 2時間 |
| チャート | 24時間 |
| API使用量 | 15分 |

Worker応答にはキャッシュ状態を示すヘッダーを付与する。

- `HIT`: Cloudflare Cache
- `KV`: Workers KV
- `MISS`: Twelve Dataへアクセス

## 12. リトライ・取得停止仕様

### 12.1 即時リトライ

| エラー | 動作 |
|---|---|
| `429` | 60秒待機し、1回だけ再試行 |
| 通信エラー | 15秒待機し、1回だけ再試行 |
| `5xx` | 15秒待機し、1回だけ再試行 |
| `400`・`404` | 即時再試行しない |
| その他の`4xx` | 即時再試行しない |

「1回だけ再試行」は、初回を含めて最大2試行を意味する。

### 12.2 停止

- `400`または`404`の場合、対象のエンドポイントと銘柄を端末の翌日0時まで停止する。
- 最終的な取得失敗が3回連続した場合、対象を6時間停止する。
- 停止中はWorkerへアクセスせず、0クレジットとする。
- 成功した場合、対象の連続失敗回数を0へ戻す。
- コルーチンのキャンセルは失敗として数えない。
- 失敗回数と停止期限は端末へ保存し、アプリ再起動後も保持する。

連続失敗の1回は、初回と即時再試行をまとめた1回の更新処理として数える。

## 13. APIクレジットログ

### 13.1 記録項目

- 日時
- HTTPメソッド
- APIパス
- HTTPステータス
- Workerキャッシュ状態
- 推定消費クレジット
- ポリシーによる取得停止

### 13.2 推定ルール

| 状態 | 推定消費 |
|---|---:|
| `HIT` | 0 |
| `KV` | 0 |
| `MISS` | APIパスの重みに応じて1または2 |
| HTTPエラー | APIパスの重みに応じて1または2 |
| `NETWORK-ERROR` | 到達した可能性があるためAPIパスの重みを計上 |
| `POLICY-BLOCK` | 0 |

端末には最大500件を保存する。設定画面から閲覧・消去できる。アプリ更新では保持し、アプリのデータ消去またはアンインストールで削除される。

推定値はTwelve Dataの請求・使用量を保証するものではない。最終的な使用量はTwelve Dataの`api_usage`を正とする。

## 14. 端末動作

### 14.1 画面点灯維持

有効時は`FLAG_KEEP_SCREEN_ON`を設定し、アプリ表示中のスリープを抑止する。

### 14.2 全画面表示

有効時はステータスバーとナビゲーションバーを隠す没入型表示を使用する。フォーカス復帰時にも全画面表示を再適用する。

### 14.3 自動起動

自動起動が有効な場合、端末の起動完了を受信してダッシュボードを起動する。

## 15. デモデータ

`debug`では外部APIへ接続せず、アプリ内のデモデータを利用する。

- APIクレジットを消費しない。
- 初期銘柄のグラフ・価格・騰落は実市場データではない。
- ローテーション、レイアウト、設定、グラフ描画の確認に使用する。
- API表示は`API DEMO`とする。

## 16. 非機能要件

### 16.1 セキュリティ

- Twelve Data APIキーをAndroidアプリへ格納しない。
- APIキーをGitへコミットしない。
- Worker本番環境ではWrangler secretを使用する。
- `local.properties`、`worker/.dev.vars`、`worker/.wrangler/`をGit管理対象外とする。

### 16.2 可用性

- ネットワークが利用できない場合でも、Roomに保存データがあれば表示を継続する。
- 一部銘柄の取得失敗で、取得済み銘柄の表示・ローテーションを停止しない。
- Activity停止時は取得処理、更新処理、ローテーションを停止する。

### 16.3 API節約

- 端末とWorkerの二段階キャッシュを利用する。
- 取得頻度制限時の再試行を1回に制限する。
- 回復しないエラーを一時停止する。
- API呼び出しをログ化し、異常消費を追跡可能とする。

## 17. 現在の制約・注意事項

- 国内投信そのものの基準価額は取得せず、ETFまたは指数を参考表示する。
- 参考指標は対象投信の厳密な値動き、信託報酬、為替ヘッジ、分配、基準価額を再現しない。
- データ供給元は設定項目として保持されるが、すべての供給元について独立した取得実装が完了しているわけではない。
- 参考指標（円換算）の厳密な円換算処理は今後の拡張対象である。
- `563A`の無料実データ供給は保証しない。
- Twelve Dataの対応市場、遅延時間、利用可能なシンボルは契約プランに依存する。
- API使用量表示はWorkerキャッシュにより最大15分程度遅れる場合がある。

## 18. 受け入れ条件

### 18.1 ダッシュボード

- 有効な銘柄が種別に応じたパネルへ表示されること。
- 2件以上の表示可能データがあるパネルは、設定間隔で継続して切り替わること。
- 通常設定でMAIN、SUB1、SUB2の切り替えが10秒ずつずれること。
- 5秒設定では3パネルが同時に切り替わること。
- プログレスバーがMAINの切り替え周期と一致すること。
- 設定変更後、アプリ再起動なしで新しい周期が反映されること。

### 18.2 キャッシュ・通信

- 2時間以内の現在値と24時間以内のチャートについて、Roomキャッシュを利用すること。
- 通信失敗時に期限切れキャッシュがあれば表示を継続すること。
- Workerの`HIT`・`KV`応答を推定0クレジットとしてログへ記録すること。
- 停止中の対象についてWorkerへアクセスしないこと。

### 18.3 リトライ

- `429`は60秒後に1回だけ再試行すること。
- 通信エラーと`5xx`は15秒後に1回だけ再試行すること。
- `400`・`404`は即時再試行しないこと。
- 連続3回失敗後は6時間アクセスを停止すること。
- 成功後は連続失敗回数がリセットされること。

### 18.4 設定

- 銘柄の追加、編集、削除、並べ替え、有効・無効が保存されること。
- 画面点灯、全画面、自動起動の設定が保存されること。
- アプリ更新時に既存設定が保持されること。
